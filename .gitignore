docs/review/
docs/proposal/

# data volumes (bulk research evidence, not tracked in git)
data/raw/
data/snapshots/
data/processed/
data/paper/
!data/metadata/

# python
__pycache__/
*.pyc
.venv/

# generated reports (audit.log / review_bundles stay tracked; paper/validation
# reports are generated evidence, not code)
reports/paper/
reports/validation/

# derived registry index (rebuilt via `pql registry rebuild`)
experiment_registry.parquet